#include <iostream>
#include <string>

// struct for patient with name and age
struct Patient {
    std::string name;
    int age;
    // in kg
    int weight;
    // in cm
    int height;
};

int main() {
// creating patients' info
    
}